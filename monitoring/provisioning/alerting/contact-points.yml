apiVersion: 1
contactPoints:
  - name: Telegram_Alerts
    receivers:
      - uid: telegram-alerts
        type: telegram
        settings:
          bottoken: "${BOT_TOKEN}"
          chatid: "${ADMINS} "
          uploadimage: true
          parseMode: HTML
